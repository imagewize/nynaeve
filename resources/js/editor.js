import domReady from '@wordpress/dom-ready';

// Import block directly
import './blocks/website-packages/index.js';

domReady(() => {
  // Block registration is handled by the block's index.js file
});